# mu-local

The embedded, daemonless, in-process engine facade — `LocalMemory` (PACKAGING-v2 §1.3).
The library form of FULL-LOCAL minus the daemon. Depends on `{mu-contracts, mu-engine}`;
never `mu-server` (CI: `mu-local-no-server`).
