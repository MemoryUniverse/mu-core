# mu-contracts

The lean shared vocabulary (PACKAGING-v2 §1.3). pydantic-only: domain models, the frozen
event catalog, ports/protocols, the error hierarchy, wire schemas, and the central Settings
tree. Depends on nothing in-project. Imported by `mu-engine`, `mu-local`, `mu-client`, `mu-server`.
