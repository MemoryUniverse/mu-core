"""``mu_engine.config`` — the intelligence-knob ``BaseSettings`` root (CONFIG-AND-DATA-FIX-PLAN.md
PART 0 §0.3, stage C0). See :mod:`mu_engine.config.engine_settings` for the full rationale.
"""

from mu_engine.config.engine_settings import EngineSettings, get_engine_settings

__all__ = ["EngineSettings", "get_engine_settings"]
