"""``python -m mu_engine.storage.migrations`` — see ``runner.py`` for the CLI itself."""

from __future__ import annotations

from mu_engine.storage.migrations.runner import main

if __name__ == "__main__":
    main()
