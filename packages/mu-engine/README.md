# mu-engine

The baseline on-device memory engine (PACKAGING-v2 §1.3, §2.1): store adapters, model
providers, engine services + OPEN baseline strategies, and the pipeline framework — all
behind the `mu-contracts` ports. Depends on `mu-contracts` only.
